const { Router } = require('express');
const express = require('express');

const router = express.Router();

function validate

const discordFunctions = {
    membercount: (req, res) => {
        if(req) {

        }
    }
}

module.exports = {
    discord:  {
        memberCount: router.route('/discord/memberCount').get(discordFunctions.membercount);
    }
}